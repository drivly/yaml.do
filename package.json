{
  "name": "yaml.do",
  "version": "1.0.0",
  "description": "Convert YAML to JSON and JSON to YAML",
  "repository": "git@github.com:drivly/yaml.do.git",
  "license": "MIT",
  "module": "worker.js",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "yaml": "^2.1.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
